import React, { Fragment } from 'react';
import PropTypes from 'prop-types';

const Watched = (props) => {
	return (
		<Fragment>
			<div>Watched</div>
		</Fragment>
	);
};

Watched.propTypes = {};

export default Watched;
